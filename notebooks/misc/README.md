# Description
For any notebooks or code that don't thematically fit into the other sub-sections of this repository.

# Notebook summaries

* 'xray_mission_coverage.ipynb' - This notebook takes the X-ray mission coverage maps generated as part of the LoVoCCS data archive, and uses them to calculate fractional coverage of variously sized apertures centered on X-LoVoCCS cluster starting coordinates.